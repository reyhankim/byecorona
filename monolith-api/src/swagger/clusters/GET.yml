Return list of clusters within an area
---
tags:
    - cluster
parameters:
    - name: longitude
      in: query
      required: true
      description: Longitude of an area of interest. In degrees. Positive means "Bujur Timur/E" where negative means "Bujur Barat/W".
      schema:
          type: number
          example: 106.8018
    - name: latitude
      in: query
      required: true
      description: Latitude of an area of interest. In degrees. Positive means "Lintang Utara/N" where negative means "Lintang Selatan/S".
      schema:
          type: number
          example: -6.2186
    - name: radius
      in: query
      required: true
      description: Radius of an area of interest, in metres
      schema:
          type: integer
          format: int64
          example: 500
operationId: getClusterList
responses:
    200:
        description: OK
        content:
            application/json:
                schema:
                    type: array
                    items:
                        type: object
                        properties:
                            cluster_id:
                                type: integer
                                format: int64
                                example: 123
                            cluster_name:
                                type: string
                                example: 'Gelora Bung Karno'
                            cctv_list:
                                type: array
                                items:
                                    type: object
                                    properties:
                                        cctv_id:
                                            type: integer
                                            format: int64
                                            example: 1
                            cluster_longitude:
                                type: number
                                example: 106.8018
                            cluster_latitude:
                                type: number
                                example: -6.2186