Flag a health protocol violation as false-positive\
---
tags:
    - violation
parameters:
    - name: violation_id
      in: query
      required: true
      description: unique ID of a health protocol violation
      schema:
          type: integer
          format: int64
          example: 1
operationId: markViolationAsFalsePositive
responses:
    200:
        description: OK
        content:
            application/json:
                schema:
                    type: object
                    properties:
                        violation_id:
                            type: integer
                            format: int64
                            example: 1